import NavBar from "./Nabvar";

const Header = (title="Bienvenido a Upayments") => {
    return (
        <div>
            <header className="App-header">
            <NavBar></NavBar>
            </header>
        </div>
    );
}

export default Header;